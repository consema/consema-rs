# Consema Python-tool fixture
[DEFAULT]
cache_dir = .cache/consema
timeout = 30

[lint]
select = E,F,I
exclude = build,
  dist,
  generated

[release]
repository: https://example.invalid/consema
raw_marker = %(package)s
