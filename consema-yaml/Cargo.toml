[package]
name = "consema-yaml"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Lossless YAML 1.2 Core and YAML 1.1 compatibility documents"

[dependencies]
consema-core = { path = "../consema-core", version = "0.6.0" }
consema-document = { path = "../consema-document", version = "0.6.0" }
consema-graph = { path = "../consema-graph", version = "0.6.0" }
saphyr-parser.workspace = true

[lints]
workspace = true
