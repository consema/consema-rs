[package]
name = "consema-conformance"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Consema language-neutral conformance vectors"
publish = false

[dependencies]
consema = { path = "../consema", version = "0.8.0" }
consema-core = { path = "../consema-core", version = "0.8.0" }
consema-document = { path = "../consema-document", version = "0.8.0" }
consema-graph = { path = "../consema-graph", version = "0.8.0" }
consema-hcl = { path = "../consema-hcl", version = "0.8.0" }
consema-ini = { path = "../consema-ini", version = "0.8.0" }
consema-json = { path = "../consema-json", version = "0.8.0" }
consema-properties = { path = "../consema-properties", version = "0.8.0" }
consema-plist = { path = "../consema-plist", version = "0.8.0" }
consema-pvce = { path = "../consema-pvce", version = "0.8.0" }
consema-protocol = { path = "../consema-protocol", version = "0.8.0" }
consema-toml = { path = "../consema-toml", version = "0.8.0" }
consema-xml = { path = "../consema-xml", version = "0.8.0" }
consema-yaml = { path = "../consema-yaml", version = "0.8.0" }

[lints]
workspace = true

[dev-dependencies]
# 0.13.0 gate plan M2 (docs/0.13.0-gate-plan.md §15.3): high-risk
# protocol/varint/offset/graph/alias logic needs property tests. Pinned to
# the exact crates.io version; MSRV 1.85 matches the workspace rust-version.
# License MIT/Apache-2.0, allowed by deny.toml.
proptest = "=1.11.0"
