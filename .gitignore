/target/
/semver-checks/
/*.pyc
/__pycache__/
/.venv/

# IDE / OS artifacts
.idea/
.vscode/
/*.swp
.DS_Store
Thumbs.db

# scratch outputs from local CLI runs (repo-root only: an unanchored
# pattern would silently ignore a committable file named cap_out.json
# anywhere in the tree, e.g. a conformance fixture or test baseline)
/cap_out.json
